<Window x:Class="Ejer5MVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Ejer5MVVM.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <vm:MainViewModel x:Key="VM"></vm:MainViewModel>
    </Window.Resources>
    <Grid DataContext="{StaticResource VM}" Margin="0,0,-0.4,-20">
        <Button Command="{Binding CheckIndice}" Content="checkindice" HorizontalAlignment="Left" Margin="106,190" VerticalAlignment="Top" Width="75"/>
        <TextBox HorizontalAlignment="Left" Height="23" Margin="203,190,0,0" TextWrapping="Wrap" Text="{Binding indice, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="120"/>
        <ListBox SelectedItem="{Binding NombreSele}" ItemsSource="{Binding Nombres}" x:Name="listnombres" HorizontalAlignment="Left" Height="150" Margin="453,30,0,0" VerticalAlignment="Top" Width="203"/>
        <TextBox HorizontalAlignment="Left" Height="114" Margin="453,250,0,0" TextWrapping="Wrap" Text="{Binding NombreSele}" VerticalAlignment="Top" Width="203"/>
        <Button Command="{Binding addname}" Content="agregar" HorizontalAlignment="Left" Margin="106,250,0,0" VerticalAlignment="Top" Width="75"/>
    </Grid>
</Window>
